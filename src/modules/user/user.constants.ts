import { ROLES } from '../../shared/constants/constants';

export const MIN_USER_NAME_CHARS = 2;
export const MAX_USER_NAME_CHARS = 60;

export const USER_FILTERS = {
  role: Object.values(ROLES),
};
